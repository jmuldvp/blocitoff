<tbody>
  <%= content_tag :tr, class: "media", id: "item-#{item.id}" do %>
    <td><%= item.name %></td>
    <td><%= link_to "", [current_user, item], method: :delete, class: 'glyphicon glyphicon-ok', remote: true %></td>
    <td><%= distance_of_time_in_words(4.days - (Time.now - item.created_at)) %></td>
    <td><%= item.created_at %></td>
    <td><%= item.id %></td>
  <% end %>
</tbody>
